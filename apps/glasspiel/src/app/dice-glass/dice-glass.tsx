import React from 'react';

import './dice-glass.scss';

/* eslint-disable-next-line */
export interface DiceGlassProps {}

export const DiceGlass = (props: DiceGlassProps) => {
  return (
    <div>
      <h1>Welcome to dice-glass component!</h1>
    </div>
  );
};

export default DiceGlass;
